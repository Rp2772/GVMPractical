import SilderComponent from "../../components/Slider/SilderComponent";

const EventListing = ({ events }) => {
  return <SilderComponent data={events} title="Events" />;
};

export default EventListing;
